version: '3'
services:
  backend:
    build:
      context: ./backend
    image: backend-image
    ports:
      - 3000:3000
  frontend:
    build:
      context: ./frontend
    image: frontend-image
    ports:
      - 4000:80